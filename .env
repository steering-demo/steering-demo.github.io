# The deployed Hugging Face Space that runs the model live.
# Public URL, not a secret. Override per build with:
#   PUBLIC_STEERING_SPACE=... npm run build
# or leave it empty to ship the recorded measurements alone.
PUBLIC_STEERING_SPACE=https://wang2226-steering-showcase.hf.space
